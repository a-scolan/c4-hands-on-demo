// --- LEGACY SPECIFICATIONS ---
// Common elements are imported from ../shared/spec.c4

specification {
  // This project has no additional specific elements
  // All elements are defined in the shared file
}
